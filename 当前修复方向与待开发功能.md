记录当前的开发进度，现存bug和瑕疵，以及待开发功能

没时间了，统计当下还存在的bug和瑕疵，下次在搞优化：

1、删除便签时点击删除要很久
2、置顶标签时，对应的便签确实置顶了，但是置顶按钮没有点亮，对用户来说不直观——标签置顶和便签手动置顶，使用的是2套排序逻辑，共用一个置顶标签的话，恐怕很容易引入bug。还没想好怎么优化。
3、点击便签检索，再次点击清除按钮时，反馈很慢。点击选择界面中的标签时，选中的速度也慢。点搜索，出结果的速度也慢。点击开关夜间模式，速度也很慢。点击便签编辑，里面也偏慢。总之，就是很卡。

处理思路：做一次特征测试锁定，然后执行性能重构方案探讨，确定后重构直到绿灯。@Refactoring\refactor_spec\FEATURE_TESTS.md
  请你读这篇文档，结合项目源代码，重新检查并更新同步特征测试的sp
  ec文档。目标是锁住当前的应用行为 ​

4、还有从全量改为增量刷新，性能反而变慢的踩坑经历：标签页置顶、批量删除、编辑便签。。这个在错题本上有记录。
5、连续快速点击排序按钮，会发现每次标题栏和功能栏都在刷新，点快了还会出现大约2秒标题和功能栏的消失。让AI修改成不刷新，死活没修改成功。

6、待开发功能：本地提醒、数据导出、WebDAV同步
